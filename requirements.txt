# Canonical source: pyproject.toml [project.dependencies].
# Polish #7.9 collapses the dual source-of-truth (drift was: numpy 2.4
# vs 1.26; pandas 2.0 vs 2.2; pydantic 2.5 vs 2.6; etc). This file
# now redirects to pyproject.toml so legacy `pip install -r requirements.txt`
# still works.
-e .
