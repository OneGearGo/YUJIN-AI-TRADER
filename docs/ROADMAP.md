# YUJIN AI TRADER · 路线图(直到上线运作)

**当前日期**:2026-06-17
**总目标**:F:\yujin-mt5\ 可 LIVE 模式跑 FTMO 10K 考核
**团队**:悟空(决策) / 铸峰(写码) / 观澜(路线图 + 验收 + 风控)
**总阶段数**:14(0-13)
**当前进度**:7/14(50%)· 上线前必做 4 阶段(8/9/10/11)

---

## 状态总览

🟢 已做(7) · 🟡 在做(0) · ⏳ 上线前必做(4) · 🚀 上线后(3)

| # | 阶段 | 状态 | 关键产出 | commit |
|---|---|---|---|---|
| 0 | 立项 + SPEC | 🟢 | SPEC.md 16 章 / 10 决策锁死 | — |
| 1 | 后端:FastAPI 入口 | 🟢 | app.py 252 行 v0.2.0 | `1b98f03` |
| 2 | 后端:MT5 桥 | 🟢 | mt5_bridge.py 180 行 · 只读 init | `1b98f03` |
| 3 | 后端:风控 | 🟢 | risk.py 113 行 · 类目系数 | `1b98f03` |
| 4 | 后端:扫描 | 🟢 | scanner.py 40 行 · 23 品种 | `1b98f03` |
| 5 | 后端:6 闸门 | 🟢 | strategy.py 221 行 · 6 gate | `1b98f03` |
| 6 | 后端:持仓 + 路由 | 🟢 | positions.py / 3 routes | `1b98f03` |
| 7 | 前端:UI 改造 | 🟢 | index.html 57538 bytes · 4 commit | `b2e450d` `d03879b` `83d1bd7` |
| 8 | **后端:数据实接** | ⏳ | .env / FTMO 真连 / scanner 真行情 | — |
| 9 | **后端:下单实接** | ⏳ | /api/buy 真单 / order_send 闭环 | — |
| 10 | **持仓监控实接** | ⏳ | mt5.positions_get / 逃生实触发 | — |
| 11 | **风控熔断实接** | ⏳ | 日亏 $300 / kill 3 / 熔断 24h | — |
| 12 | 文档 + 验收 | ⏳ | README / .env.example / Docker | — |
| 13 | 上线 · 模式切 LIVE | 🚀 | FTMO 10K 考核 / 监控 7×24 | — |

---

## Phase 8 · 后端数据实接(上线前 blocker #1)

**目标**:SHADOW 模式跑真行情,LIVE 模式跑真下单
**预计代码量**:~250 行
**依赖**:悟空填 .env(FTMO 模拟账号 + path)

### 工作项

- [ ] .env 填 MT5 凭据(FTMO 模拟账号 + path)
- [ ] `.env.example` 加详细注释(脱敏模板)
- [ ] `app.py` 启动时连接 MT5(只读 init,不踢 GUI)
- [ ] `/api/screen` 走 `scanner.scan_24()` 拉真 M15 数据
- [ ] 6 闸门跑真数据(EMA / FVG / BOS / DXY / RiskAmount)
- [ ] 数据返回结构跟 SHADOW 一致(前端无需改)
- [ ] 单测覆盖 scanner + 6 闸门(用 fixture mock MT5)
- [ ] 错误处理:MT5 断开自动 reconnect(指数退避)

### 完成标准

- `/api/screen` 返回 14 个品种的真实 M15 数据
- 6 闸门输出与种子数据形态一致(空仓/全过/部分过)
- `.env` 不入 git(.gitignore 已配)
- 单测全绿,覆盖率 ≥ 60%
- 断开重连:MT5 终端重启后 30s 内自动恢复

### 观澜验收点

- 真连 vs 模拟数据形态对齐(品种数 / 闸门状态 / 优先级)
- 凭据不在代码 / 文档 / 日志
- 重启后自动 reconnect(3 次测试)
- `.env` 模板填空位 + 注释,悟空能照填

---

## Phase 9 · 后端下单实接(上线前 blocker #2)

**目标**:点"确认买入" → 真单 → 真回执
**预计代码量**:~200 行
**依赖**:Phase 8 完成

### 工作项

- [ ] `/api/buy` 接 `mt5.order_send`(下完 `mt5.shutdown` 释放 GUI 占用)
- [ ] `/api/sell` 同上
- [ ] 订单回执解析(success / partial / reject / requote)
- [ ] 失败时回滚 UI 状态(乐观锁)
- [ ] /api/unmonitor 从监控池移除
- [ ] 单测覆盖 order_send 路径(mt5 mock)
- [ ] 集成测试:demo 账号下 0.01 lot → 终端见单

### 完成标准

- demo 账号点 BUY → MT5 终端见单 → UI 显示 ticket 号
- reject 单据展示原因(余额不足 / 品种关闭 / requote)
- 拒单 / 部分成交不更新持仓
- /api/buy 平均响应 < 500ms

### 观澜验收点

- demo 单据(0.01 lot)在终端可见
- 网络断开不卡 UI(超时 5s 自动报错)
- 并发下单不重复(同一 sym 互斥锁)

---

## Phase 10 · 持仓监控实接(上线前 blocker #3)

**目标**:持仓 card 显真单 + 逃生信号实触发
**预计代码量**:~180 行
**依赖**:Phase 9 完成

### 工作项

- [ ] `positions.py` 走 `mt5.positions_get()` 1m 轮询
- [ ] severity 算法(滑点 / 持仓时间 / 集中度 / 趋势反转)
- [ ] severity ≥ 70 自动弹窗"是否平仓"(30s 内)
- [ ] 持仓 card 接 `openPanel('pos', ticket)` 详情(UI 配合改)
- [ ] 后端 WebSocket 推送(避免前端轮询)

### 完成标准

- 持仓 card 显真单 sym / lots / pnl / severity
- 1m 刷新一次(WS 推送 < 100ms)
- 逃生信号 30s 内出(severity 70 阈值)
- 持仓详情面板可看滑点历史 / 集中度

### 观澜验收点

- 真单 vs 种子数据形态一致
- 熔断时逃生按钮禁用
- WS 断线自动重连

---

## Phase 11 · 风控熔断实接(上线前 blocker #4)

**目标**:日亏 $300 / 连亏 3 次 → 拒单 + 报警
**预计代码量**:~150 行
**依赖**:Phase 10 完成

### 工作项

- [ ] `risk.py` 加日亏统计(读 deal history,算 sum)
- [ ] kill switch 状态机(NORMAL / WARNING / TRIPPED)
- [ ] 顶栏状态切"熔断"(红色 + 倒计时 24h)
- [ ] 报警:声音(必) + 微信(可选) + 邮件(可选)
- [ ] 熔断解除:人工确认 / 24h 到期
- [ ] 单测覆盖日亏 / kill / 熔断状态机

### 完成标准

- 日亏 = $300 触发拒单 + 报警
- 连亏 3 次触发 kill switch(全平 + 锁 24h)
- 顶栏状态实时更新
- 报警 ≤ 3s 出(声音 / UI 弹窗)

### 观澜验收点

- 模拟触发 1 次,UI 状态正确切换
- 24h 倒计时不漂
- kill 后 5min 内新单被拒
- 测 3 次熔断,每次都正确恢复

---

## Phase 12 · 文档 + 验收(上线前)

**目标**:新 clone → 看 README → 5 分钟跑起来
**预计代码量**:~100 行(README/Dockerfile)
**依赖**:Phase 8-11 全部完成

### 工作项

- [ ] README 完善(快速开始 / 凭据 / 模式 / 路线图链接)
- [ ] `.env.example` 详细注释(已在 Phase 8)
- [ ] `Dockerfile`(可选,v0.3)
- [ ] `pytest` 单测覆盖核心(已有)
- [ ] 演示 GIF / 截图(README 嵌入)
- [ ] `ROADMAP.md` 同步更新(本文件)

### 观澜验收

- 新 clone 的人 5 分钟内能跑起来(用朋友/同事测一次)
- 公开 URL 仍可访问(demo 数据)
- 所有命令一键复制即跑

---

## Phase 13 · 上线 · 模式切 LIVE(上线后)

**目标**:FTMO 10K 考核
**预计代码量**:0 行(纯运营)
**依赖**:悟空授权切 LIVE

### 工作项

- [ ] 悟空授权切 LIVE
- [ ] .env 填 FTMO 真凭据
- [ ] 监控 7×24(本地服务 / GitHub Actions 备份)
- [ ] 每日 22:00 CST 自动备份 ~/.hermes/ 到 F 盘
- [ ] 周报(WR / 净 PnL / 最大回撤 / Sharpe)
- [ ] 触发规则:连续 3 天触发 kill → 强制 review + 参数回滚

### 观澜验收

- 第 1 周日亏 ≤ $100 / WR ≥ 50%
- 0 次熔断误触发
- 1 次真单成交(验证下单链路)
- 周报自动生成

---

## 责任分工(铸峰 vs 观澜 vs 悟空)

| 角色 | 做什么 | 不做什么 |
|---|---|---|
| **悟空**(决策) | 授权切 LIVE / 改 V4.5 集成决策 / 大方向 / .env 凭据 | 不写代码(已委托铸峰) |
| **铸峰**(写码) | Phase 8-12 所有写码 / 单测 / 集成 / 提交 PR | 不做路线图 / 不做验收 / 不改 UI 文案 |
| **观澜**(验收) | ROADMAP 维护 / 每 PR 验收 / 风险检查 / 单元数据 / 周报 | 不写代码(Phase 7 是悟空特别授权) |

---

## 当前最阻塞(3 件,需要悟空决定)

1. **.env 凭据** — Phase 8 起点,悟空填 FTMO 模拟账号 + path
2. **MT5 终端装好** — F:\MT5\terminal64.exe 路径在不在?demo 账号登了没?
3. **V4.5 集成决策** — Phase 13 之前要定:接 V4.5 / V4-PRO / 还是新写 v0.2?

---

## 风险登记

| 风险 | 缓解 |
|---|---|
| 凭据泄露 | 不进对话 / 不入代码 / 不入 git(`.env` 强 ignore) |
| GMGN 借鉴范围 | 形态/语言 OK,具体代码/文案/配色不照搬(形态借鉴不复用代码) |
| V4.5 边界 | 集成前先讨论,集成后单测覆盖,SOUL 铁律"V4.5 不动" |
| kill switch 失灵 | Phase 11 必测 3 次,1 次失败回滚 |
| MT5 终端断连 | 自动 reconnect + 心跳监测 + 三层防御(熔断/恢复/断连) |
| 并发下单冲突 | 同一 sym 互斥锁(Phase 9 实现) |
| 公开 URL 暴露策略 | 保留 demo 数据(脱敏),不暴露真凭据 / 真持仓 |
