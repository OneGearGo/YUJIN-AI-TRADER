# YUJIN AI TRADER · 工作日记(2026-06-18)

## 一句话
接续 2026-06-17 Phase 7 尾巴(持仓 card / 表格行 接 openPanel 第二层),补 commodity 类目,收工于 WORKLOG。

## 今天 commit 链
- `7d63c35` `feat(panel): 接 openPanel sym 品种详情第二层(2026-06-18)` — 实际是 A+B 二者合并(原计划 split 4 commit 但 git 暂存顺序错位)
- `f365d36` `feat(symbols): 补 commodity 3 个(XTIUSD/XAGUSD/XCUUSD) + totalScanned 14 到 17(2026-06-18)`

字节数:static/index.html 57538(上) → 60395(今 commit C 后)930 行

## 关键改动(A+B 仓合并)

### A · 持仓 card 接 openPanel
- `.pos-card` `cursor:pointer`(行 128)
- 持仓 card `<div class="pos-card ${radCls}">` 抓 `data-sym="${p.sym}"` + `onclick="openPanel('pos',event)"`(原行 632)
- `openPanel` 函数体 拆 `if(ev){...}` 一行为 `let sym=null; if(ev){ closest('[data-k]') + closest('[data-sym]') 捕获 sym}(原行 734)
- 新增 'pos' 分支:POS.find → renderPosPanel
- `renderPosPanel(p)` 详表(品种/方向/手数/入场/现价/止损/止盈/浮盈亏/严重度/信号)插在 ESC 监听后(原行 745 后)
- CSS stub 撑样式:`.panel-grid` / `.panel-cell` / `label b .wide` / `.severity-bar i` / `.empty`(原行 284 前)

### B · 表格行 接 openPanel
- `renderScreenTable` row click handler `openDetail(r.dataset.sym)` → `openPanel('sym',e)`(原行 622)
- 新增 'sym' 分支:SEED_SYMBOLS.find → renderSymPanel
- `renderSymPanel(d)` 详表(品种/类目/状态/置信度/优先级/点差/BOS/DXY + 6 闸门 gate-grid)插在 renderPosPanel 后
- CSS stub 撑 闸门:`.gate-grid` 6 列 + `.g-cell.{pass|fail|skip}` 三色

### C · 补 commodity
- XAUUSD 类目 `forex` → `commodity`(gold 归位)
- SEED_SYMBOLS 加 XTIUSD(XTIUSD 原油) / XAGUSD(白银) / XCUUSD(铜) 三个 commodity 条目
- totalScanned 14 → 17
- funnelData 同步 `{1:14,2:8,3:5,4:4,5:3,6:2}` → `{1:17,2:12,3:8,4:6,5:4,6:3}`

## 部署状态(本地)
- HTTP 200 OK,62059+字节(随 patch 增长)
- 退后按 push 需悟空在 bash 走 `git -c credential.helper='!f() { echo username=OneGearGo; echo password=ghp_TOK; }; f' push origin main`

## 未完成 / 明日接续
1. 视觉验收 — browser-use 拉一下看板,点 pos-card / 点表格行 走一遍,看面板是否如期弹出
2. MT5 真接 —悟空 填 `.env` 凭据,启本地 uvicorn 走 `/api/run` + `/api/buy`
3. 清理 openDetail 遗留 —目前已无引用,可顺手删防死代码
4. v0.2 递进:LLM 闸门 v0.1 → v0.2 + POST /api/run 带合同
5. 公众号 / 监控 / 自动备份 (远期)

## 风险 / 教训
- git 暂存+commit 顺序错位,A 与 B 被合并到 7d63c35(原本意图拆分 4 commit 各独立小粒度)
- 用 `git -C /f/yujin-mt5 ...` 替代 `cd ... && git ...`(Git Bash 子 shell 偶发丢失 cwd)
- Python 不能依赖 `python3`(某些走 `/c/Python314/python`);如果 PATH 有问题 走 `python` 可能是 windows launcher
- Emoji 在 Python 源码中用 *literal* 穿过 bash heredoc 是 OK 的,但要注意 `\uXXXX`(4 位)会被截成 `\u0001`+文本,用 `\UXXXXXXXX`(8 位)才会被读作代码点
- 单 HTML 中插入多行 闭合 </script> 必须一次至多一个(昨日教训复盘)
- ”每天 commit / push 不囤"被提醒 (这种小错误下次要避开)

## 明日待办(2026-06-19)
- v0.2 · MT5 真接 + LLM 闸门
- 视觉验收(browers-run 走一圈面板)
- 验货悟空后能 push
